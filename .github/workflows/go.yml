name: Go

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:

  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2

    - name: Set up Go
      uses: actions/setup-go@v2
      with:
        go-version: 1.18beta1

    - name: Build
      env:
        GOOS: windows
        GOARCH: amd64
      run: go build -o auth-server.exe ./dist/ -v .
    - name: Upload a Build Artifact
      uses: actions/upload-artifact@v3.0.0
      with:
        path: auth-server.exe
